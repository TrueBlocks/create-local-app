{
  "$schema": "https://wails.io/schemas/config.v2.json",
  "name": "{{ORG_NAME}} {{PROJECT_PROPER}}",
  "outputfilename": "{{SLUG}}",
  "frontend:install": "yarn install",
  "frontend:build": "yarn build",
  "frontend:dev:watcher": "yarn dev",
  "frontend:dev:serverUrl": "http://localhost:5173",
  "author": {
    "name": "{{ORG_NAME}}",
    "email": "info@{{DOMAIN}}"
  }
}
